# 2026-09-02

## UI/UX 质感升级（Graphite 体系深化，四波）

- 深色色板冷调墨化：`src/App.css` .dark 组从中性炭灰改蓝调墨——background #15171d、card/sidebar #1d1f27、popover #262931、muted #20232b、accent #272a34、secondary #2a2d37、border #31343f、input #434754；层级规则不变（越高层越亮）。亮色组未动。
- 全局 `::selection` 改为主题蓝 26% 混合（App.css @layer base）。
- 浮层通透化：context-menu / dropdown-menu(+sub) / select 弹层、undo toast、拖拽幽灵、git-branches 错误弹层统一 `bg-popover/90 backdrop-blur-xl`；contextual-action-bar `bg-popover/85`；entry-preview /95 + blur-xl；dialog 遮罩 `backdrop-blur-sm`。
- 微交互：Button 基元 `active:scale-[0.97]`（transition 补 scale）；侧边栏 NavItem、标签页 `active:scale-[0.98]`；LocationCard 与 projects-section 卡片 hover `-translate-y-0.5 + shadow-ambient-sm`、按压回弹归位。
- 排版：file-list 行 rounded-xs→rounded-md；侧边栏 SectionLabel 与 workspace SectionHeader 落实 text-label 约定（uppercase + tracking 0.05em）。
- 同步点：index.html 启动屏 dark 底 #15171d、spinner 边 #434754；terminal-panel.tsx token 回退值、terminal-palette.ts 注释与 dark ANSI black（#31343f）对齐。
- 验证：tsc ✓、oxlint 0 警告 ✓、vite build ✓（先 rm -rf dist 规避 emptyOutDir 沙箱坑）。
- 教训：改 :root/.dark token 后必须 grep 硬编码 hex（index.html 启动屏、终端回退、ANSI 注释）保持同步。
